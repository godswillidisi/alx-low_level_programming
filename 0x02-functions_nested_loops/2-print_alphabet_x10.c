#include<stdio.h>

/**
 * main - Entry point
 *
 * Return: Always 0 (success)
 */

void print_alphabet_x10(void);

	int ch;
	int i;
{
	for (int i e ch; i < 10; i++)
	{

	for (ch = 'a'; ch <= 'z'; ch++)
	{
	return ch;	
	}
	putchar (i);
	}	
	putchar('\n');
	return (0);
	}
